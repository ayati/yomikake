# 空行の詰め方（設計書）

Web 小説にありがちな「装飾としての空行」を、読む側で詰めて表示する機能。
対象は **リフロー本のみ**（FXL は画像なので概念が無い）。**両ファイル共通**。

---

## 1. 何が問題か（実測）

Web の投稿サイトは**組版機能が乏しい**ため、会話文の前後や段落ごとに**空行を入れて誌面の
呼吸を作る**作法が広まっている。PC のブラウザで読むぶんには雰囲気が出るが、**スマホの
狭い画面では1画面の半分以上が空白**になり、ページ送りの回数が倍近くに増える。
紙の本は同じことを**組版（行間・アキ）**で処理するので、詩集などの例外を除けば空行を多用しない。

yomikake は行間・字間・余白を設定できるので、**この空行は「組版の代用」であって作品の一部
ではない**と解釈できる。ただし紙の本の「場面転換の1行アキ」は作品の一部なので、**区別が要る**。

### 1-1. 蔵書 20 冊の実測（2026-09-21）

| 本 | 空行率 | 連続の長さ | 性格 |
|---|---|---|---|
| ねらわれた学園（新装版） | 2.2% | ほぼ 1 | 紙の組版（場面転換） |
| RAIL＿WARS！9 | 2.7% | 1 と 3 | 同上 |
| スーパーカブ | 14.9% | 1 と 2 | 中間 |
| 平安朝の事件簿 | 16.7% | ほぼ 1 | 新書（節のアキ） |
| 仏教入門 | 19.0% | ほぼ 1 | 同上 |
| 蘇我氏 | 25.6% | ほぼ 1 | 同上 |
| **僕は異世界で美幼女姫様と…** | **42.7%** | **1**（19,750） | Web 装飾（1行アキ型） |
| **薬屋のひとりごと** | **45.3%** | **1**（36,178） | 同上 |
| **１０歳から始める冒険者生活** | **51.7%** | **2**（1,317） | Web 装飾（2行アキ型） |
| **じゃない方の白石くん** | **60.4%** | **2**（12,407） | 同上 |
| **学校を休みたかった俺に…** | **66.5%** | **2**（8,422） | 同上 |

**紙の本 2〜26% / Web 装飾 43〜67% で、分布が大きく離れている。**
空行の表現は novel_downloader 産が `<p class="body-blank">&#160;</p>`、商業本は素の `<p>` に
空白 1 文字など、**本によって違うのでクラス名に依存した実装はできない**。

### 1-2. 効き方の試算（1行30字・1画面14行＝スマホ縦持ち相当・全文）

| 本 | そのまま | 連続空行を1行に | すべて詰める |
|---|---|---|---|
| じゃない方の白石くん | 4,447画面 | 3,533（79%） | 2,586（**58%**） |
| １０歳から始める冒険者生活 | 443画面 | 349（79%） | 255（**58%**） |
| 薬屋のひとりごと | 7,880画面 | 7,761（**98%＝効かない**） | 5,138（65%） |
| ねらわれた学園 | 279画面 | 279（100%） | 275（99%） |
| RAIL＿WARS！9 | 408画面 | 406（100%） | 400（98%） |

- **「連続を1行に」だけでは 1行アキ型（薬屋・僕は異世界）に一切効かない。**
- **「すべて詰める」だけだと紙の本の場面転換も消える**（ただし効果自体が 1〜2% なので実害は小さい）。
- → **3 段階が過不足ない。**

---

## 2. 仕様

### 2-1. 設定「空行の詰め方」`state.blankLines`

| 値 | ラベル | 動作 |
|---|---|---|
| `auto` | おまかせ | 本ごとに空行の入り方を測って自動で決める（§5） |
| `off`（既定） | そのまま | 何もしない |
| `collapse` | 連続空行を1行に | 2行以上続く空行のうち **2 行目以降**を隠す |
| `all` | すべて詰める | 空行をすべて隠す |

### 2-2. 設定「会話の前後を半行あける」`state.dialogueGap`（既定 OFF・独立トグル）

行頭が `「` `『` の段落と、そうでない段落の**境目にだけ** `padding-block-start` を入れる。

- **「前後の両方」にしない。** 会話が連続する箇所にも空きが入り、**詰めたのに間延びする**。
  紙の組版でも会話の連続は詰める。境目に入れれば「地の文 → 会話」「会話 → 地の文」の
  両方が1回ずつ空く。
- 量は **`state.lineHeight × 0.5` em**（行間 2.0 なら 1em ＝ ちょうど半行）。
  `lh` 単位は古い iOS Safari で使えないので、描画時に em へ焼く。
- **`margin` ではなく `padding` を使う。** 隣接ブロックのマージンは相殺されるため、
  ePub 側が `margin-block-end` を持っていると**指定しても何も起きない**ことがある。
  padding は相殺されず、背景も枠線も無い本文では見た目は同じ。
- 空行を詰めていないとき（`off`）にも単独で使える。
- **新しい本は必ず OFF で始まる（`applyBookPrefs` の先頭で落とす）。** ほかの項目と違い、
  前の本の設定を持ち越すと**組版済みの紙の本の組版を黙って変えてしまう**。その本に
  明示的な記録があるときだけ復元する（旧ビルドで開いていた本も記録が無いので OFF）。
  「空行の詰め方」のほうは持ち越す —— 紙の本に適用しても効果が 1〜2% しかなく無害で、
  Web 小説を続けて読むときに毎回設定し直さずに済むため。
- コストは実測 +4〜10 ポイント（白石くん 58%→62%、薬屋 65%→75%）。

### 2-3. 判定規則（クラス名に依存しない一般則）

**空行**＝ 次をすべて満たすブロック:
- テキストから空白類（半角/全角スペース・NBSP・タブ・改行・ZWSP・BOM）を除くと空
- `img` / `svg` / `hr` / `table` / `video` / `audio` / `canvas` / `input` / `object` / `embed` を含まない
- **`id` 属性を持たない** —— 目次・検索・kosync の着地点になりうるものは残す。
  `display:none` の要素は `scrollIntoView` が効かないため、隠すと黙って章頭に落ちる。
  実測5冊で id 付きの空行は **0 件**なので、この安全弁のコストは無い。

**走査対象**は `p,div,h1〜h6,blockquote,li,pre,figure,td,th` のうち、**同種の子を持たないもの**
（＝いちばん内側のブロック）。章全体を包む `div` を数えると「中身が全部空行の div」まで
空行になり、連続の数え方も狂う。

**連続の数え方**は走査順（＝文書順）。空行でないブロックで run をリセットする。

**会話の判定**は行頭の 1 文字が `「` または `『`。`（` は心の声・補足の両方に使われ、
**地の文の途中に置かれることが多い**ので入れない（必要になったら足せる）。

### 2-4. 扱わないもの（初版）

- 段落**内**の `<br><br>`（＝1つの `<p>` の中の連続改行）。蔵書 20 冊で **0 件**。
- 自動 ON。§5 の提案トーストは段階2。

---

## 3. 実装方式：**消さずに隠す**（設計の核心）

`buildSrcdoc()` の中で本文ブロックを **1 回**走査し、空行に `class="yk-bl"`（連続 2 つ目以降は
`yk-bl-x` も）、会話の境目に `class="yk-gap"` を**付けるだけ**。実際に消すのは注入 CSS。

```
collapse → .yk-bl-x{display:none!important}
all      → .yk-bl  {display:none!important}
会話半行 → .yk-gap {padding-block-start:<lineHeight×0.5>em!important}
```

**⚠ `<p>` を実際に削除してはいけない。**

1. **kosync の XPointer は「同名兄弟の N 番目」で位置を指す**（`koNthChildByTag`）。段落を
   消すと添字がずれ、**KOReader から受け取った位置が黙って別の段落に着地する**。
   DOM の形を保てばこの問題は発生しない。
2. 目次・全文検索・アンカー（`#id`）のジャンプ先が生き残る。
3. 段階の切替が CSS の差し替えだけで済み、走査は 1 回のまま。

走査は `state.blankLines !== 'off' || state.dialogueGap` のときだけ行う（既定では完全に無費用）。

**読み上げ・外部への受け渡し・全文検索・しおり書き出しは生の ePub（ZIP 内の XHTML）を読む**ので
いずれも無影響。原文は 1 文字も書き換えない。

---

## 4. 設定の置き場所とスコープ

- 設定パネル **「タイポグラフィ」**グループ（字間の下）に 2 行。`.fxl-hide-row` を付けて
  FXL では出さない。変更時は `rerenderKeepPos()`（読書位置を保つ）。
- **本ごとに記憶する**（`epub_book_prefs` に `blankLines` / `dialogueGap` を追加し 5 項目 → 7 項目）。
  `design_per_book_settings.md` の線引き「**本が変われば変わるべきか**」にそのまま当てはまる
  —— Web 小説か紙の本かは**本の属性**であって、読者の目や環境の話ではない。
- `change*()` はグローバルと本ごとの**両方**に書く（既存の作法。本ごとにだけ書くと
  グローバルが初期値で固定され、新しい本を開くたびに設定し直すことになる）。
- `DISPLAY_DEFAULTS` に入れる＝表示設定リセットの対象。

---

## 5. おまかせ（自動判定）

ユーザーは普通、最初に表示を決めたらそれ以上設定を開かない。**紙の本はそのまま・毎回空行が
入る Web 小説は詰める**を自動で選ぶ。

### 5-1. 判定に使う 3 つの数（蔵書 13 冊の実測・2026-09-21）

| 本 | 空行/本文 | 会話の前に空行 | 地の文どうしの間に空行 | 判定 |
|---|---|---|---|---|
| ねらわれた学園 | 0.02 | 0% | 5% | そのまま |
| RAIL＿WARS！9 | 0.02 | 1% | 2% | そのまま |
| 地下室からのふしぎな旅 | 0.00 | 0% | 0% | そのまま |
| スーパーカブ | 0.17 | 0% | 12% | そのまま |
| 仏教入門 | 0.16 | 15% | 17% | そのまま |
| 平安朝の事件簿 | 0.20 | 33% | 16% | そのまま |
| 蘇我氏 | 0.26 | 6% | 23% | そのまま |
| 僕は異世界で… | 0.62 | 69% | 29% | 詰める |
| 薬屋のひとりごと | 0.83 | 71% | 76% | 詰める |
| **１０歳から始める冒険者生活** | **1.21** | **100%** | **4%** | **詰める＋半行アキ** |
| じゃない方の白石くん | 1.47 | 99% | 55% | 詰める |
| 学校を休みたかった俺に | 1.91 | 100% | 95% | 詰める |

（数値は実装のサンプリング＝最大12章・本文300ブロックで打ち切り、で測ったもの）

- **空行/本文比**が紙・新書 0.00〜0.26、Web 装飾 0.62〜1.91 で**2倍以上離れている**。
  しきい値 **0.5** なら誤爆しない。
- **地の文どうしの間に空行があるか**が「意味を持つ空行」かどうかを分ける。
  １０歳から始める冒険者生活は**会話の前は100%・地の文どうしは4%**＝空行が
  「会話の区切り」という意味を持っている。この本だけ、詰めたあとに半行アキで呼吸を戻す。

### 5-2. 決め方

| 条件 | 空行の詰め方 |
|---|---|
| 空行/本文比 < `BLANK_AUTO_RATIO`(0.5) | そのまま |
| ≥ 0.5 かつ 1画面 ≤ `BLANK_AUTO_NARROW`(25) 行 | すべて詰める |
| ≥ 0.5 かつ 1画面 > 25 行 | 連続空行を1行に |

| 条件 | 会話の半行アキ |
|---|---|
| 「すべて詰める」になり、会話の前に空行 ≥ 0.70 かつ 地の文どうし < 0.25 | ON |
| それ以外 | OFF |

- **「狭い画面」は px ではなく「1画面に入る行数」**（`読書エリアの長さ ÷ 文字サイズ×行間`）。
  **文字を大きくしている人は PC でも狭い扱い**になり、実際の読みにくさに連動する。
- **半行アキは「すべて詰める」のときだけ。** 「連続を1行に」では会話まわりの空行が1行
  残っているので、そこへ半行を足すと二重になる。
- 本文ブロックが `BLANK_AUTO_MIN_BLOCKS`(150) に届かない本は**判定しない**（短編・詩集）。

### 5-3. 測り方

`measureBlankStyle()` が本文のある章を**最大12章ぶん候補にし、本文300ブロックが
取れた時点で打ち切る**。ふつうの Web 小説は2〜4章で足り、章が短い新書でも材料が集まる。
**`markBlankBlocks()`（描く側）と同じ `_blankLeafBlocks()` / `_isBlankBlock()` を使う** ——
見方がずれると「測った本と描く本が違う」ことになる。

### 5-3b. 実装で踏んだ落とし穴（コードレビューで発見・2026-09-21）

- **`linesPerScreen()` が `display:none` の `#page-container` を測っていた。** `applyBlankAuto()` は
  `.visible` が付く前に走るので 0×0 が返り、**必ず「狭い画面」と判定されて `collapse` 分岐が
  死んでいた**（1600px の窓でも「すべて詰める」）。実寸を持つ親へ落として解決。
- **`toggleDialogueGap()` を本未オープンで触ると方針が消えた。** 固定する先の本が無いのに
  `state.blankLines` を書き換えていたため、直後の `saveSettings()` が `'off'` を保存していた。
- **UI 同期の抜け。** `toggleDialogueGap()` の固定と `applyBookPrefs()` の早期 return が
  セレクトを置き去りにしていた（`changeBlankLines` で塞いだのと同じ穴）。`syncBlankUI()` に一本化。
- **`resetDisplaySettings()` が `_blankPinned` を落としていなかった** ——
  `saveSettings()` が `'auto'` を書き戻し、リセットが効かなかった。
- **`_isBlankBlock()` が自分の `id` しか見ていなかった。** `<p><a id="note1"></a></p>` のような
  脚注の戻り先が隠れ、**目次・脚注のジャンプが黙って章頭に落ちる**。子孫の `[id],a[name]` も見る。
- **`li` / `td` / `th` を空行扱いしていた。** `display:none` にすると `<ol>` の番号が繰り上がり、
  表はセルが行から消えて以降の列がずれる。`BLANK_NEVER_SEL` で除外（走査対象には残すので
  連続の数え方は正しいまま）。

### 5-4. UI の約束

- 判定結果は `state` に書かず `_blankAuto = {level, gap}` に持つ。**設定欄は「おまかせ」のまま**。
  端末が変われば（スマホ↔PC）その端末に合った結果になる。`epub_book_prefs` は端末ローカル
  なのでこの挙動と整合する。
- **黙って見た目を変えない。** 詰めたときは**その本で一度だけ**トーストで告知し、
  **タップでその本だけ「そのまま」に戻せる**（`showBlankAutoToast`）。
- **戻したあと、もう一度おまかせにする道を残す。** その本を開いたまま設定の
  「空行の詰め方」で**「おまかせ」を選び直す**と再判定され、本ごとの記録も `auto` に戻る。
  そのために 2 つが要る:
  - **⚠ `changeBlankLines()` はセレクトの値も同期する。** この関数は告知トーストからも
    呼ばれるので、同期しないと**「設定欄はおまかせのまま・中身は固定」という嘘の状態**になる。
    セレクトが既に `auto` を表示していると**同じ値を選んでも change イベントが飛ばず、
    戻す手段が消える**（実機の導線を追って判明）。
  - **設定パネルに現在の状態を1行出す**（`updateBlankNoteUI` / `#blank-auto-note`）。
    「おまかせ：この本は『すべて詰める』にしています」「この本だけ『そのまま』に固定しています
    （「おまかせ」を選び直すと自動に戻ります）」。おまかせは結果が見えないと理解できない。
    `data-i18n` を持たない動的テキストなので `applyI18n()` の末尾からも呼ぶ。
- **手で選び直した本は以後その本だけ固定。このときグローバルは「おまかせ」のまま保つ** ——
  1冊を直したせいでライブラリ全体の方針が解除されるのは意図と違う。
  「最後に使った設定が新しい本の既定になる」という既存の作法への、**ここだけの例外**。
  印は `_blankPinned` で持ち、**`saveSettings()` が `blankLines` だけ `'auto'` を書き戻す**。
  `changeBlankLines` の中だけで済ませると、**固定中にテーマなど他の設定を触っただけで
  方針が消える**（実装中に踏んだ）。**本を開いていないとき（読みかけリスト）の変更は
  方針そのものの変更**として扱う —— 固定する先の本が無いため。
- 既定は `off` のまま（おまかせは選んだ人に効く）。FXL では測らない。

## 6. テスト

`tests/cases/blank-lines.js`（両ファイル各 50 assertion）。fixture は**新しい 2 冊**
`tests/.fixtures/blank.epub` と `blankweb.epub`（おまかせ用・会話の前後だけ空ける Web 小説型）を足す（既存の `reflow.epub` を触ると e2e・TTS・kosync が
影響を受けるため）。章には次を入れる:

- `&#160;` だけの `<p>`（2 連続・1 つだけ・3 連続）
- `<p><br/></p>` 型
- `id` 付きの空行（**隠れないこと**の確認用）
- 会話（`「`）と地の文の並び
- 画像だけの `<p>`（空行と誤判定しないこと）

検査は**実際に本を開いて iframe の中の実寸を測る**（クラスの有無だけを見ない。
2026-09-21 の「よく読む著者」の棒グラフが、クラスは正しいのに幅 0 だった教訓）。
