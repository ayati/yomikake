# Google Drive 同期の起動時アカウント選択をなくす 設計書

対象: `yomikake.html`・`yomikake_ios.html` **両ファイル**（`driveAuth()` 周辺）

関連: CLAUDE.md §Google Drive Bookmark Sync・`design_kosync.md`（資格情報を別キーに置く前例）

**状態: 実装完了・実機確認待ち（2026-09-23）。** テスト `tests/cases/drive-auth.js`（両ファイル各 44 assertion）。決定事項: §3-2（OFF で記憶を消す）・§3-4（iOS は最初の click／EPUB_TAP に相乗り）。 実測は 2026-09-23（PC・Android・iPad・iPhone）。iOS の「最初のタップに相乗り」も測定済み。

---

## 1. 何が困っているか

自動同期を ON にしていると、**yomikake を開くたびに「Google でログイン — アカウントを選択」が出てタップを求められる**。
PC はタブを開きっぱなしにすれば避けられるが、スマホでは毎回になる。「さあ読むぞ」の瞬間に操作を強いられるのがストレス。

### 原因

- 起動時に `driveSyncPull({force:true})` → `driveAuth()` が走る。
- ページを読み込み直すと `_tokenClient` が空に戻るので、`driveAuth()` は毎回「初回」の分岐を通り、
  **`requestAccessToken()` を `prompt` 指定なしで呼ぶ**。GIS はこのときアカウント選択を出す。
- `prompt:''`（サイレント更新）を使っていたのは、同じページのセッション内での 2 回目以降だけだった。

GIS のトークン方式はリフレッシュトークンを持てない（アクセストークンの寿命は 1 時間）。
そのため「ポップアップを一切出さずに取り直す」ことはブラウザだけではできない。
ここは変えずに、**タップを要求しない呼び方に変える**のが今回の範囲。

---

## 2. 実測（`tests/probe/drive-silent-auth.html`）

条件は `prompt` と `hint`（メールアドレス）の組み合わせ 5 通り。
「ボタンで実行」（ユーザー操作あり）と「起動時に実行」（タブを閉じて開き直した直後・ユーザー操作なし）の両方で測った。
数値はトークン取得までの時間。

### 2-1. PC（Windows・Chrome 153）

| 条件 | ボタン | 起動時 | 見えたもの |
|---|---|---|---|
| `[A]` prompt 未指定・hint なし（**現行**） | 8.2 s | 4.0 s | アカウント選択（要タップ） |
| `[B]` `prompt:''` | 0.6 s | 0.6 s | 一瞬チラつく |
| `[C]` `prompt:''` ＋ hint | 0.6 s | 0.5 s | 一瞬チラつく |
| `[D]` `prompt:'none'` ＋ hint | 0.6 s | 失敗※ | ボタン: 一瞬チラつく |
| `[E]` prompt 未指定 ＋ hint | 5.6 s | 3.6 s | アカウント選択（要タップ） |

※ `popup_failed_to_open`。実行の瞬間にタブが裏にあった（`visibility=visible` が 2 秒後）ため。Android では起動時も成功している。

### 2-2. Android（Android 10・Chrome 153・ブラウザのタブ）

| 条件 | ボタン | 起動時 | ログから読めること |
|---|---|---|---|
| `[A]`（**現行**） | 5.3 s | 4.8 s | アカウント選択（要タップ） |
| `[B]` | 1.2 s | 1.2 s | 別タブに切り替わり、自動で戻る |
| `[C]` | 1.2 s | 1.2 s | 同上 |
| `[D]` | 1.2 s | 1.5 s | 同上（起動時も成功） |
| `[E]` | 4.7 s | 4.5 s | アカウント選択（要タップ） |

### 2-3. iPad（iPadOS 18.7・Safari 26.6.1・ブラウザのタブ）

| 条件 | ボタン | 起動時 |
|---|---|---|
| `[A]`（**現行**） | 4.4 s（アカウント選択） | **`popup_failed_to_open`** |
| `[B]` | 0.5 s | **`popup_failed_to_open`** |
| `[C]` | 0.5 s | **`popup_failed_to_open`** |
| `[D]` | 0.5 s | **`popup_failed_to_open`** |
| `[E]` | 3.5 s（アカウント選択） | **`popup_failed_to_open`** |

- ボタンからなら PC・Android と同じく `prompt:''` でタップ不要（約 0.5 秒）。
- **ユーザー操作なしの起動直後は、条件にかかわらずすべてブロックされる。**
- ⚠ **つまり iOS では、現行コードの起動時同期が毎回失敗している。** しかも失敗は `error_callback` にしか届かないので、
  §3-3 の不具合により `_authPromise` が固まる。`driveSyncPull` は `_driveSyncBusy` を立てたまま戻らず、
  自動保存（`runAutoSave`）も同じ Promise を待って `_autoSaveBusy` が立ちっぱなしになる。
  コード上は **iOS では自動同期を ON にしていても、起動後は Drive への上げ下げが一切行われていない**はず（要実機確認）。
- **ご本人の体感（2026-09-23）: iPhone でも自動同期はしていたと思う。** probe（Safari のタブ）の結果と食い違う。考えられる理由は 2 つ。
  (1) 普段はホーム画面から起動した standalone で使っていて、ポップアップの扱いがタブと違う（probe は `standalone=false` でしか測っていない）。
  (2) GIS の読み込み待ち（`driveSyncPull` は最大 6 秒待つ）の間にタップしていて、たまたまユーザー操作の文脈内で `requestAccessToken` が呼ばれていた。
  → **iPhone は普段ホーム画面から起動している（ご本人・2026-09-23）ので (1) が有力。** standalone の実測はまだ無い。
  **どちらでも §3-4 の作りで吸収できる**（起動時に通ればそれでよく、ブロックされたら最初のタップで取り直す）。実機確認で「起動直後に出るか／最初のタップで出るか」を記録して決着させる。

### 2-4. 読み取れること

- **アカウント選択の原因は `prompt` 未指定。hint だけでは消えない**（`[E]`）。
- **`prompt:''` を付ければタップは要らない**。残るのは PC で約 0.5 秒、Android で約 1.2 秒の「タブが切り替わって戻る」動き。
  → **ご本人の判断: 操作を強要されないので許容できる**（2026-09-23）。
- ユーザー操作なしの起動直後でも、ポップアップはブロックされなかった（PC・Android とも）。
- `[F]`: 今の権限（`drive.appdata`）のままで `drive/v3/about?fields=user(emailAddress)` からメールアドレスを取れる。hint を手入力させる必要はない。
- `[B]` と `[C]` に差は出なかった（測定した端末は Google アカウントが 1 つだけと思われる）。
  hint は**複数アカウントの端末で選択画面を出さないための保険**として付ける。

**iPhone（iOS 18.7・Safari 26.5.2）も完全に同じ**: ボタンなら `[B]`〜`[D]` が 0.5〜0.6 s、`[A]` `[E]` はアカウント選択、
起動時は全条件 `popup_failed_to_open`、`[F]` 取得可。

### 2-5. iOS: 最初のタップに相乗りできるか

開き直したあと、最初のタップで `[C]` を実行した。

| 札 | どのタップで実行したか | 結果 | `userActivation.isActive` |
|---|---|---|---|
| `[G]` | 親画面の `click` | **成功**（0.9 s／2.4 s） | true |
| `[H]` | 親画面の `touchend` | **`popup_failed_to_open`** | **false** |
| `[I]` | 本文 iframe の `click` → postMessage → 親（**yomikake の `EPUB_TAP` と同じ経路**） | **成功**（0.6 s） | true |
| `[J]` | 本文 iframe の `click` → 親の関数を直接 | **成功**（0.6 s） | true |

数値は iPad。**iPhone も同じ結果**（`[G]` 0.7 s・`[H]` 失敗・`[I]` 0.6 s・`[J]` 0.6 s）。

- **本文 iframe 内のタップは親に伝わる**。iframe で起きたユーザー操作の文脈は親ドキュメントにも及ぶので、
  postMessage を受けた時点でも親でポップアップを開ける。**既存の `EPUB_TAP` 経路にそのまま相乗りできる**（`[J]` のような新しい経路は要らない）。
- **`touchend` は使えない**。iPadOS の Safari では `touchend` の時点でユーザー操作の文脈がまだ立っていない。
  相乗りは `click`（と `keydown`）で行う。
- スワイプ（iOS 版の iframe 内 `touchstart`/`touchend`）では開けないと考えられる。最初の操作がスワイプなら、次のタップまで待つ。

### 2-6. 未測定

- Android の**ホーム画面にインストールした PWA**。standalone ではポップアップの開き方が変わりうる。実装後の実機確認で見る。

---

## 3. 設計

### 3-1. 「この端末は一度許可を済ませた」を覚える

新しい localStorage キー **`epub_drive_account`**: `{ email: string, t: ISO }`

- **トークンを取れたら**書く。`email` はその直後に `about?fields=user(emailAddress)` で取る。
  取得に失敗しても `email:''` で書く（許可済みの印としては有効。`[B]` 相当で動く）。
- `driveAuth()` の初回分岐は、このキーがあれば **`prompt:''` ＋ `hint: email`**（`[C]`）、無ければ従来どおり prompt 未指定。
- `[D]`（`prompt:'none'`）は採らない。許可が取り消されていた場合、`[D]` はエラーで返るだけだが、`[C]` なら Google が必要な画面（同意・ログイン）を出してくれるので、利用者が自力で復帰できる。

**しおりとは別キー**にする。`collectBookmarks()` の走査対象（`epub_pos_*` / `epub_last_book`）から構造的に外れるので、
JSON 書き出しにも Drive にも載らない（`epub_kosync` と同じ作法）。保存するのはメールアドレスだけで、トークンは今までどおりメモリにだけ持つ。

### 3-2. アカウントの記憶を消す契機

hint を覚えると、別のアカウントに切り替える手段が要る。

- **決定（2026-09-23）: 自動同期を OFF にしたら `epub_drive_account` を消す。** 次に ON にするときは、今と同じくアカウント選択から始まる。
  「OFF → ON でアカウントを選び直せる」は説明しやすく、UI を増やさずに済む。
- 不採用: 設定の Google Drive グループに「アカウントの記憶を消す」ボタンを置く。明示的だが、使う機会がほぼ無い UI が 1 つ増える。
- 自動保存が回復不能で止まったとき（`runAutoSave` の `toast.driveAutoSaveExpired` 経路）も消す。
  その後に手動で ON にし直す流れが、そのままアカウント選び直しになる。

### 3-3. 既存の不具合: 認証ポップアップを閉じると `driveAuth()` が永久に固まる（同時に直す）

`initTokenClient` に `error_callback` を渡していない。ポップアップを閉じた（`popup_closed`）とき、
または開けなかった（`popup_failed_to_open`）とき、GIS は `callback` を呼ばない。
そのため **`_authPromise` が解決も破棄もされないまま残り、以後の `driveAuth()` は全部その固まった Promise を返す**。
結果として、ページを読み込み直すまで Drive のボタンが何をしても反応しなくなる。

**ご本人の体感で裏付けあり（2026-09-23）**: 「iPhone で認証画面を閉じたりしてこけると Drive と切れていた」「Android でうっかり認証画面を落とすと Chrome を強制終了して立ち上げ直していた」。
どちらもこの不具合の症状そのもの（ページを読み込み直すまで戻らない）。

今回の変更で起動時に認証が走る頻度は変わらないが、iOS では起動時に開けない（§2-3）ので、現にこの穴に落ちている。

- `error_callback` で `_authPromise = null` にして reject する。
- `driveSyncPull()` は元から例外を握りつぶすので、起動時に失敗しても黙って終わる。手動の上げ下げボタンは既存のエラートーストに乗る。

### 3-4. iOS の扱い

起動直後はブロックされる（§2-3）。→ **起動時の `driveSyncPull` を「最初のユーザー操作まで待たせる」**。
操作の中から同期的に `requestAccessToken` まで呼べばポップアップは開ける（ボタンでは通っている）。
読み始めの 1 タップに相乗りするので、利用者の操作は増えない。

- **相乗りする操作（§2-5 で確定）**: 親ドキュメントの `click`・`keydown`（capture で先に拾う）、および **`EPUB_TAP` / `EPUB_KEY` の受信**。
  **`touchend` は使わない**（iPad で文脈が立っていない）。
- 起動時にまず普通に試し、**`popup_failed_to_open` で失敗したときだけ**「最初の操作で取り直す」を仕掛ける。
  取れたら仕掛けを外す。取れなかった（別の理由で失敗した）ら、次の操作でまた試す。
  iOS では起動時の試行が 1〜2 ms で失敗するだけなので、無駄はない。
- 取り直しの間に `EPUB_POS` などで自動保存が走っても、`driveAuth()` が固まらない（§3-3）ので、失敗して `_autoSaveDirty` に持ち越されるだけ。
  トークンが取れた後の最初の保存で送られる。
- ⚠ **ユーザー操作の文脈を切らさないこと。** 操作のハンドラから `requestAccessToken` までの間に `await` を挟めない。
  GIS が読み込み済みで、かつトークンが無いときだけ同期で呼ぶ。
- 起動直後に取れなかったときに固まらないことは §3-3 で担保する。
- **両ファイルに入れる。** PC でも、タブが裏で開いたときは `popup_failed_to_open` になった（§2-1 の `[D]`）。
  「起動時に失敗したら最初の操作で取り直す」は PC・Android にも無害な保険になる。

### 3-5. 変えないもの

- トークンはメモリのみ（localStorage に置かない）。
- 同セッション内の期限前サイレント更新（`scheduleTokenRefresh`）はそのまま。ここでも hint を渡すようにするだけ。
- 手動の ⬆ / ⬇ ボタンも同じ `driveAuth()` を通るので、許可済みの端末ではアカウント選択が出なくなる。

---

## 4. テスト

`tests/cases/drive-auth.js`（両ファイル）。`window.google.accounts.oauth2.initTokenClient` と `fetch` を差し替えて見る。

- `epub_drive_account` が無ければ `requestAccessToken` を prompt 未指定で呼ぶ／あれば `prompt:''` と `hint` を渡す
- トークン取得後に `about` を読み、`epub_drive_account` に email を書く。`about` が失敗しても `email:''` で書く
- `error_callback` 経由で失敗したら reject され、**次の `driveAuth()` が新しい認証を始める**（固まらない）
- 起動時に `popup_failed_to_open` → 親の `click` ／ `EPUB_TAP` の受信で `requestAccessToken` が**同期で**呼ばれる。成功後は仕掛けが外れる
- `touchend` では取り直さない
- 自動同期 OFF で `epub_drive_account` が消える
- `collectBookmarks()` の結果に `epub_drive_account` が含まれない

**担保できないこと**: 実際にポップアップが出るか・チラつきの程度・iOS でブロックされるか。いずれも実機で確認する。

---

## 5. 実機確認（ご本人）

| 環境 | 見ること |
|---|---|
| PC Chrome | 開き直しでアカウント選択が出ない／OFF→ON で選び直せる |
| Android Chrome（タブ） | 同上。約 1.2 秒で自動で戻る |
| Android（ホーム画面の PWA） | 同上。standalone でもポップアップが開いて戻る |
| iPhone / iPad | 起動直後は何も出ない → 最初のタップ（本文でもリストでも）で一瞬チラついて同期される。最初がスワイプなら次のタップで。**これまで iOS で自動同期が動いていなかったか**（§2-3）もあわせて確認 |

---

## 6. 採らなかった案

| 案 | 理由 |
|---|---|
| サーバー側でリフレッシュトークンを持つ | チラつきまで消せるが、サーバーでトークンを保管する責任が生じる。同意画面を「本番」に切り替える作業も要る。チラつきは許容と判断したので見送り。**将来チラつきが問題になったら、これが次の一手** |
| トークンを localStorage に 1 時間保持 | 1 時間以内に開き直したときしか効かない。「トークンはメモリにだけ持つ」方針も崩れる |
| Android ネイティブアプリ化 | Google は WebView 内の OAuth を禁止しているので、認証をネイティブで書き直す必要がある。しかも Android にしか効かない |
| `prompt:'none'`（`[D]`） | 許可が切れていたときに利用者が自力で復帰できない（§3-1） |
